/**
 * protocols/ArchiveFormat.cpp
 *
 * WARNING: This file is autogenerated. To make changes, edit autogen_config.yml instead and re-generate.
 * Codegen hash (sha256): ef5d919892d7fb2dfe245e886f16af756d82c23434f16d91c2ddd4537b82261e
 *
 * (C) Copyright 2014-present  Cristian Dinu <goc9000@gmail.com>
 *
 * This file is part of UniArchive II.
 *
 * Licensed under the GPL-3
 */

#include "protocols/ArchiveFormat.h"
#include "utils/qt/shortcuts.h"
#include "utils/serialization/deserialization_helpers.h"

#include <QMap>
#include <QtDebug>

namespace uniarchive2 { namespace protocols {

using namespace uniarchive2::utils::serialization;

QString name_for_archive_format(ArchiveFormat format) {
    switch (format) {
        case ArchiveFormat::INVALID:
            return "(invalid)";
        case ArchiveFormat::ADIUM:
            return "Adium";
        case ArchiveFormat::DIGSBY:
            return "Digsby";
        case ArchiveFormat::FACEBOOK_DYI:
            return "Facebook (DYI archive)";
        case ArchiveFormat::MSN_MESSENGER_XML:
            return "MSN Messenger (XML format)";
        case ArchiveFormat::PIDGIN_HTML:
            return "Pidgin (HTML format)";
        case ArchiveFormat::PIDGIN_TXT:
            return "Pidgin (TXT format)";
        case ArchiveFormat::SKYPE:
            return "Skype";
        case ArchiveFormat::WHATSAPP_EMAIL:
            return "WhatsApp (Email format)";
        case ArchiveFormat::YAHOO_MESSENGER_DAT:
            return "Yahoo Messenger (DAT format)";
    }

    invariant_violation("Invalid ArchiveFormat value (%lld)", (int64_t)format);
}

QString symbol_for_archive_format(ArchiveFormat format) {
    switch (format) {
        case ArchiveFormat::INVALID:
            return "invalid";
        case ArchiveFormat::ADIUM:
            return "adium";
        case ArchiveFormat::DIGSBY:
            return "digsby";
        case ArchiveFormat::FACEBOOK_DYI:
            return "facebook_dyi";
        case ArchiveFormat::MSN_MESSENGER_XML:
            return "msn_messenger_xml";
        case ArchiveFormat::PIDGIN_HTML:
            return "pidgin_html";
        case ArchiveFormat::PIDGIN_TXT:
            return "pidgin_txt";
        case ArchiveFormat::SKYPE:
            return "skype";
        case ArchiveFormat::WHATSAPP_EMAIL:
            return "whatsapp_email";
        case ArchiveFormat::YAHOO_MESSENGER_DAT:
            return "yahoo_messenger_dat";
    }

    invariant_violation("Invalid ArchiveFormat value (%lld)", (int64_t)format);
}

ArchiveFormat archive_format_from_symbol(IMM(QString) symbol) {
    const static QMap<QString, ArchiveFormat> MAP = {
        { "invalid",             ArchiveFormat::INVALID },
        { "adium",               ArchiveFormat::ADIUM },
        { "digsby",              ArchiveFormat::DIGSBY },
        { "facebook_dyi",        ArchiveFormat::FACEBOOK_DYI },
        { "msn_messenger_xml",   ArchiveFormat::MSN_MESSENGER_XML },
        { "pidgin_html",         ArchiveFormat::PIDGIN_HTML },
        { "pidgin_txt",          ArchiveFormat::PIDGIN_TXT },
        { "skype",               ArchiveFormat::SKYPE },
        { "whatsapp_email",      ArchiveFormat::WHATSAPP_EMAIL },
        { "yahoo_messenger_dat", ArchiveFormat::YAHOO_MESSENGER_DAT },
    };

    if (MAP.contains(symbol)) {
        return MAP[symbol];
    }

    invariant_violation("Invalid ArchiveFormat symbol ('%s')", QP(symbol));
}

QDebug operator<< (QDebug stream, ArchiveFormat format) {
    stream << QP(name_for_archive_format(format));

    return stream;
}

QDataStream& operator>> (QDataStream& mut_stream, ArchiveFormat& mut_format) {
    uint8_t code = must_deserialize(mut_stream, uint8_t);

    switch ((ArchiveFormat)code) {
        case ArchiveFormat::INVALID:
        case ArchiveFormat::ADIUM:
        case ArchiveFormat::DIGSBY:
        case ArchiveFormat::FACEBOOK_DYI:
        case ArchiveFormat::MSN_MESSENGER_XML:
        case ArchiveFormat::PIDGIN_HTML:
        case ArchiveFormat::PIDGIN_TXT:
        case ArchiveFormat::SKYPE:
        case ArchiveFormat::WHATSAPP_EMAIL:
        case ArchiveFormat::YAHOO_MESSENGER_DAT:
            mut_format = (ArchiveFormat)code;
            break;
        default:
            invariant_violation("Invalid deserialized ArchiveFormat value (%lld)", (int64_t)code);
    }

    return mut_stream;
}

QDataStream& operator<< (QDataStream& mut_stream, ArchiveFormat format) {
    mut_stream << (uint8_t)format;

    return mut_stream;
}

}}
