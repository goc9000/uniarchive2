/**
 * intermediate_format/events/file_transfer/RawReceiveFileEvent.cpp
 *
 * WARNING: This file is autogenerated. To make changes, edit autogen_config.yml instead and re-generate.
 * Codegen hash (sha256): fc3b237a15a378c01dd5a812ae4da4cd41709232bdfa57090b9ecac30c5f23c9
 *
 * (C) Copyright 2014-present  Cristian Dinu <goc9000@gmail.com>
 *
 * This file is part of UniArchive II.
 *
 * Licensed under the GPL-3
 */

#include "intermediate_format/events/file_transfer/RawReceiveFileEvent.h"
#include "intermediate_format/subjects/visitor/visit_subject_utils.h"
#include "utils/qt/debug_extras.h"
#include "utils/serialization/deserialization_helpers.h"
#include "utils/serialization/serialization_helpers.h"

namespace uniarchive2 { namespace intermediate_format { namespace events {

using namespace uniarchive2::utils::serialization;

RawReceiveFileEvent::RawReceiveFileEvent(
    IMM(ApparentTime) timestamp,
    uint32_t index,
    TAKE(ApparentSubject) receiver,
    IMM(RawTransferredFile) file
) : RawEvent(timestamp, index), receiver(move(receiver)), file(file) {
    sanityCheckMandatoryParameters();
}

void RawReceiveFileEvent::sanityCheckMandatoryParameters() const {
    invariant(receiver, "Parameter 'receiver' cannot have empty value");
}

RawEventSubType RawReceiveFileEvent::subType() const {
    return RawEventSubType::RECEIVE_FILE;
}

bool RawReceiveFileEvent::visitSubjectsImpl(IApparentSubjectVisitor& visitor) {
    return visit_subjects(receiver, visitor) && visit_subjects(sender, visitor);
}

CEDE(RawReceiveFileEvent) RawReceiveFileEvent::deserializeFromStream(QDataStream& mut_stream, bool skip_type) {
    maybeDeserializeType(skip_type, mut_stream, RawEventSubType::RECEIVE_FILE);

    ApparentTime timestamp = must_deserialize(mut_stream, ApparentTime);
    uint32_t index = must_deserialize(mut_stream, uint32_t);
    optional<QByteArray> skype_guid = must_deserialize(mut_stream, optional<QByteArray>);
    unique_ptr<ApparentSubject> receiver = must_deserialize(mut_stream, unique_ptr<ApparentSubject>);
    unique_ptr<ApparentSubject> sender = must_deserialize_optional_unique_ptr(mut_stream, ApparentSubject);
    RawTransferredFile file = must_deserialize(mut_stream, RawTransferredFile);

    unique_ptr<RawReceiveFileEvent> event = make_unique<RawReceiveFileEvent>(timestamp, index, move(receiver), file);
    event->skypeGUID = skype_guid;
    event->sender = move(sender);

    return event;
}

void RawReceiveFileEvent::serializeDetailsToStream(QDataStream& mut_stream) const {
    mut_stream << receiver;
    serialize_optional_unique_ptr(mut_stream, sender);
    mut_stream << file;
}

void RawReceiveFileEvent::writeDetailsToDebugStream(QDebug stream) const {
    stream << " receiver=" << receiver;
    if (sender) {
        stream << " sender=" << sender;
    }
    stream << " file=" << file;
}

}}}
